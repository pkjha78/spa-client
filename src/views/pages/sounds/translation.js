import {getLocalizedStrings} from "../../../shared/utility";

let strings = getLocalizedStrings({
  en: {
    id: "Id",
    name: "Name",
    created: "Created",
    updated: "Updated",
    action: "Action"
  }
});

export default strings;
